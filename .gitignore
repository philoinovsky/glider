# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# custom
.idea
.vscode
.zed
.DS_Store

# dev test only
/dev/
dev*.go

# Upstream ignores *_test.go wholesale. This fork tracks tests for the code it
# adds (dial retry, forwarder status endpoint), which has no upstream regression
# net of its own.

dist

*.zip
/*.conf
/*.rule

config/rules.d/*.rule
config/rules.d/*.list

glider
/bak/
/rules.d/

CLAUDE.md